server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Serve static frontend from /usr/share/nginx/html (mounted from ./frontend)
    location / {
        # Serve static files when they exist; otherwise fall back to SPA entry.
        try_files $uri $uri/index.html /index.html;
    }
}

